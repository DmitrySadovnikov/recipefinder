<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Document</title>
</head>
<body>

<h1>Searching for - <%= @search_term %></h1>

<table border="1">
  <tr>
    <th>Image</th>
    <th>Recipe Name</th>
    <th>Rank</th>
  </tr>
  <% @recipes.each do |recipe| %>
      <tr class=<%= cycle('even', 'odd') %>>
        <td><%= link_to(image_tag(recipe["image_url"], height:"100",width:"100"),recipe["f2f_url"]) %></td>
        <td><%= link_to(recipe["title"],recipe["f2f_url"]) %></td>
        <td><%= recipe["social_rank"] %></td>
      </tr>
  <% end %>
</table>

<p>You can change your search term</p>
<p>.../recipes/index?search=swiss</p>

<p>Powered By Food2Fork.com</p>


</body>
</html>




